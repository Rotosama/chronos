<nav class="navbar navbar-expand-lg bg-dark"> <!-- Reemplaza "bg-custom-color" con la clase de color personalizada que desees -->
  <div class="container">
    <%= link_to "Inicio", root_path, class: "navbar-brand text-white" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Departamentos", departments_path, class: "nav-link text-white" %>
        </li>
        <li class="nav-item">
          <%= link_to "Empleados", workers_path, class: "nav-link text-white" %>
        </li>
        <li class="nav-item">
          <% if current_worker %>
            <%= link_to "Vacaciones", worker_vacations_path(current_worker), class: "nav-link text-white" %>
          <% end %>
          </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Vacaciones
          </a>
          <ul class="dropdown-menu dropdown-menu-dark">
            <li><a class="dropdown-item" href="#">Ver vacaciones</a></li>
            <li><a class="dropdown-item" href="#">Aprobar vacaciones action</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="#">Something else here</a></li>
          </ul>
        </li>
      </ul>
      <% if worker_signed_in? %>
        <%= link_to edit_worker_path(current_worker), class: "text-white" do %>
          <i class="bi bi-person-circle mx-2"></i><%= current_worker.name.capitalize %>
          <% end %>
        <%= link_to destroy_worker_session_path, data: { turbo_method: :delete }, class: "btn btn-danger ms-1" do %>
            <i class="bi bi-door-closed-fill"></i>Cerrar Sesión
          <% end %>
      <% else %>
        Invitado
      <% end %>
    </div>
  </div>
</nav>
